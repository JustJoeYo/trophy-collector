import PlayerSearch from '../components/PlayerSearch'

export default function PlayerPage() {
    return (
        <main>
            <h1>Player</h1>
            <PlayerSearch />
        </main>
    )
}